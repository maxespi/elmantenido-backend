<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/admin.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <h1>📊 Tablas de Base de Datos</h1>
        <nav>
          <a href="/admin">← Dashboard</a>
          <a href="/admin/users">Usuarios</a>
          <a href="/admin/tables">Tablas</a>
          <a href="/api/health" target="_blank">API Health</a>
        </nav>
      </header>

      <main>
        <div class="table-container">
          <div class="table-header">
            <h2>🗄️ Gestión de Tablas</h2>
          </div>

          <div class="tables-grid">
            <% tables.forEach(table => { %>
            <div class="table-card">
              <div class="table-icon"><%= table.icon %></div>
              <div class="table-info">
                <h3><%= table.displayName %></h3>
                <p>Tabla: <code><%= table.name %></code></p>
              </div>
              <div class="table-actions">
                <a href="/admin/table/<%= table.name %>" class="btn"
                  >Ver Datos</a
                >
                <a
                  href="/admin/table/<%= table.name %>/create"
                  class="btn btn-success"
                  >Agregar</a
                >
              </div>
            </div>
            <% }) %>
          </div>
        </div>
      </main>
    </div>

    <style>
      .tables-grid {
        padding: 30px;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 20px;
      }

      .table-card {
        border: 2px solid #e2e8f0;
        border-radius: 10px;
        padding: 25px;
        display: flex;
        align-items: center;
        gap: 20px;
        transition: all 0.3s;
      }

      .table-card:hover {
        border-color: #667eea;
        transform: translateY(-2px);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      }

      .table-icon {
        font-size: 3rem;
        flex-shrink: 0;
      }

      .table-info {
        flex: 1;
      }

      .table-info h3 {
        margin-bottom: 5px;
        color: #4a5568;
        font-size: 1.3rem;
      }

      .table-info code {
        background: #f7fafc;
        padding: 2px 6px;
        border-radius: 3px;
        font-size: 0.85rem;
        color: #667eea;
      }

      .table-actions {
        display: flex;
        flex-direction: column;
        gap: 8px;
      }

      .btn-success {
        background: #48bb78;
      }
    </style>
  </body>
</html>
